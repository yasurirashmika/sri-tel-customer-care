spring:
  application:
    name: chat-service
  
  datasource:
    url: jdbc:postgresql://localhost:5432/chat_db
    username: postgres
    password: Yasuri@123
    driver-class-name: org.postgresql.Driver
  
  jpa:
    hibernate:
      ddl-auto: create-drop  # Temporarily recreate schema - change back to 'update' after first run
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true

server:
  port: 8083

# Gemini API Configuration
gemini:
  api:
    key: AIzaSyBfT55WHmHgqFqpddiML_53D2BhedDWK98
    url: https://generativelanguage.googleapis.com/v1beta/models/gemini-1.5-flash:generateContent
  simulation:
    enabled: true  # Enable fallback simulation mode

logging:
  level:
    lk.sritel.chat: DEBUG
    org.springframework.web.socket: DEBUG
